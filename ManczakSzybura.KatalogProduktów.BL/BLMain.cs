using MańczakSzybura.KatalogProduktów.INTERFACES;
using System.Reflection;

namespace ManczakSzybura.KatalogProduktów.BL
{
    public class BLMain
    {
        public void setup()
        {
            //var dllPath = config["DaoLibrary"]; // odczyt z JSON
            //var asm = Assembly.LoadFrom(dllPath);

            //// Znajdź wszystkie klasy implementujące ICPU
            //var cpuType = typeof(ICpu);
            //var types = asm.GetTypes().Where(t => cpuType.IsAssignableFrom(t) && t.IsClass);

            //foreach (var t in types)
            //{
            //    var cpu = (ICpu)Activator.CreateInstance(t);
            //}



        }
    }
}
